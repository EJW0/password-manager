# password-manager
A simple password manager built in Java.

## Usage
Run PasswordApplication.java to launch the GUI. On your first use, enter a main password that will be used to encrypt the passwords file. Add passwords using the GUI.